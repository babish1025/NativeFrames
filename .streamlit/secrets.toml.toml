# Copy these values into Streamlit Cloud -> Settings -> Secrets.
# Do NOT commit real passwords or database URLs to GitHub.

NATIVEFRAMES_DATABASE_URL = "mysql://u_vKlaXU:H5oodL9UHMHc@sql.freedb.tech:3307/freedb_nHmfR453"
NATIVEFRAMES_DB_AUTO_CREATE = "false"
NATIVEFRAMES_ADMIN_USERNAME = "nativeframes"
NATIVEFRAMES_ADMIN_PASSWORD = "nativeframes@24"

# Optional: use a persistent disk path on a cloud host that provides one.
# NATIVEFRAMES_UPLOAD_DIR = "/var/data/nativeframes/uploads"
